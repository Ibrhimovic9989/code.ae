// Where the main workspace app lives — the "start building" buttons point here.
// Override per environment via NEXT_PUBLIC_APP_URL on Vercel.
export const AppUrl = process.env['NEXT_PUBLIC_APP_URL'] ?? 'https://app.code.ae';
